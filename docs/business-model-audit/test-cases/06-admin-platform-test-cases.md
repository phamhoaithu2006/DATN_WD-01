# Test Case — Nền tảng quản trị

## Phạm vi

- Business Rules: `BR-085`–`BR-096`.
- Các case gắn BUG dùng kết quả mong đợi của Business Model; cột kết quả cũng ghi rõ hành vi source hiện tại làm case thất bại.
- “Hiện trạng tự động hóa” chỉ ghi test đang tồn tại khi test đó có assertion trực tiếp cho case; không quy đổi việc endpoint được gọi gián tiếp thành coverage.

## Danh sách test case

| ID | Business Rule | Mục tiêu | Loại test | Tiền điều kiện và dữ liệu | Các bước | Kết quả mong đợi | Source Code Reference | Hiện trạng tự động hóa |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-SA-085-01 | BR-085 | Tạo backup MySQL/MariaDB bằng mysqldump | Service Integration / Happy path | Driver `mysql` hoặc `mariadb`; binary khả dụng; connection hợp lệ | Gọi `DatabaseBackupService::createBackup()` | Chạy `mysqldump` timeout 300 giây với các option source; tạo file dưới `storage/app/backups` tên đúng regex và trả metadata | `DatabaseBackupService.php:11-138`, `createBackup()`, `backupDirectory()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-085-02 | BR-085 | Từ chối driver ngoài MySQL/MariaDB | API/Service / Validation | Admin; driver `pgsql` | POST `/api/admin/backups` | HTTP `422`; không tạo file | `DatabaseBackupService.php:77-83`; `DatabaseBackupController.php:28-45` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-085-03 | BR-085 | List, download và delete file hợp lệ | API Feature / CRUD file | Admin; file `vivugo-backup-20260101-010101.sql` tồn tại | Gọi GET list, GET download, DELETE | List chứa filename; download `200`; delete `200` và file biến mất | `DatabaseBackupService.php:40-74,140-169`; controller `index/download/destroy` | `backend_laravel/tests/Feature/DatabaseBackupApiTest.php` — `admin can list download and delete backup files`; assert list filename, download `200`, delete `200`, file không tồn tại |
| TC-SA-085-04 | BR-085 | Chặn filename không đúng pattern | API Feature / Security validation | Admin; filename `not-a-backup.sql` hoặc path traversal | Gọi download/delete | HTTP `404`; không đọc/xóa file ngoài pattern | `DatabaseBackupService.php:13,155-161`; `DatabaseBackupController.php:47-76` | `DatabaseBackupApiTest.php` — `admin cannot access invalid backup filenames`; assert download filename sai trả `404` |
| TC-SA-085-05 | BR-085 | Dọn file dở khi dump lỗi | Service Integration / Exception | Process trả exit code lỗi sau khi tạo output file | Gọi create backup | Ném `RuntimeException`; output file bị xóa | `DatabaseBackupService.php:28-37,111-136`, `createBackup()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-085-06 | BR-085 | API manual backup gọi service và prune mặc định | API Feature / Orchestration | Admin; mock service trả metadata; chưa có retention setting | POST `/api/admin/backups` | HTTP `201`; create gọi một lần; prune gọi với 7; response có filename | `DatabaseBackupController.php:28-45`, `store()` | `DatabaseBackupApiTest.php` — `admin can create a manual backup`; mock assert `createBackup()` once, `pruneOldBackups(7)` once, response `201` và filename |
| TC-SA-085-07 | BR-085 | Chỉ admin truy cập backup API | API Feature / Authorization | Guest và customer | GET `/api/admin/backups` | Guest `401`; customer `403` | `routes/api.php:208-211`; `CheckRole.php` | `DatabaseBackupApiTest.php` — `backup APIs require authentication` assert `401`; `backup APIs require admin role` assert `403` |
| TC-SA-086-01 | BR-086 | Daily backup chỉ chạy sau giờ và một lần mỗi kỳ | Command Integration / Schedule | Settings enabled, daily, time `02:00`; clock cùng ngày sau 02:00; cache kỳ rỗng | Chạy `db:backup --scheduled` hai lần | Lần đầu create + prune + cache forever; lần hai không tạo thêm file vì period key đã cache | `DatabaseBackupCommand.php:12-88`, `handle()`, `shouldRunScheduledBackup()`; `routes/console.php:10` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-086-02 | BR-086 | Bỏ qua khi tắt hoặc chưa đến giờ | Command Integration / Alternative | Data set: enabled=false; enabled=true nhưng clock trước backup_time | Chạy scheduled command | Trả success, không gọi create/prune, không ghi cache kỳ | `DatabaseBackupCommand.php:31-72` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-086-03 | BR-086 | Đúng kỳ weekly/monthly | Command Integration / Boundary time | Weekly: Monday sau giờ; Tuesday; Monthly: ngày 1 sau giờ; ngày 2 | Chạy command từng bộ | Chỉ Monday và ngày 1 đủ điều kiện; period key tương ứng năm-tuần hoặc năm-tháng | `DatabaseBackupCommand.php:55-88`, `shouldRunScheduledBackup()`, `scheduledPeriodKey()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-086-04 | BR-086 | Prune theo retention setting | Command Integration / Filesystem | Retention 3 ngày; có file cũ hơn và mới hơn threshold | Chạy kỳ scheduled hợp lệ | Sau create, file quá hạn bị xóa; file trong hạn được giữ | `DatabaseBackupCommand.php:37-45`; `DatabaseBackupService.php:76-104`, `pruneOldBackups()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-086-05 | BR-086 | Validation cấu hình backup | API Feature / Validation | Admin; frequency yearly, time `25:00`, retention 0 | PUT `/api/admin/settings` | HTTP `422` với ba field; không ghi giá trị sai | `SettingController.php:56-86`; `Setting::ALLOWED_KEYS` | `backend_laravel/tests/Feature/BackupSettingsTest.php` — `backup settings are validated`; assert `422` và errors `backup_frequency`, `backup_time`, `backup_retention_days` |
| TC-SA-086-06 | BR-086 | Ghi cấu hình backup hợp lệ | API Feature / Settings | Admin; enabled, daily, 02:00, retention 7 | PUT settings | HTTP `200`; bốn key được lưu, group backup | `SettingController.php:21-103`, `update()` | `BackupSettingsTest.php` — `admin can save backup settings`; assert response values và DB key `auto_backup_enabled`, group `backup` |
| TC-SA-086-07 | BR-086 | Ghi nhận concurrent scheduler không có lock | Concurrency / Idempotency characterization | Hai process cùng kỳ, cache chưa có, cùng vượt qua check trước khi process kia ghi cache | Chạy hai scheduled commands đồng thời | Cả hai đi vào create/prune; source không dùng `withoutOverlapping`, distributed lock hoặc atomic cache add | `routes/console.php:10`; `DatabaseBackupCommand.php:31-46,64-72` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-087-01 | BR-087 | Validation message/session ở biên | API Feature / Boundary | Guest; message 1.000 và session 100, sau đó 1.001/101/thiếu message | POST `/api/chatbot` | Payload đúng biên được xử lý; vượt biên/thiếu trả `422` đúng field | `ChatBotController.php:15-20`, `handleChat()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-087-02 | BR-087 | Sinh guest session ổn định từ IP/User-Agent | API Feature / Session | Không gửi session_id; cố định IP và User-Agent | Gọi hai request | session ID bằng `guest-` + MD5 dữ liệu ghép trong source; chỉ một conversation theo unique session; mỗi lần thêm messages | `ChatBotController.php:22-32`; `backend_laravel/database/migrations/2026_07_15_193903_create_chat_conversations_table.php` unique session | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-087-03 | BR-087 | Gắn user chỉ khi tạo conversation mới | API Feature / Identity | Conversation session X đã tồn tại với user A/null; request sau xác thực user B cùng X | Gọi chatbot với X | `firstOrCreate` trả row cũ và không đổi user_id; session mới khi B gọi được tạo với B | `ChatBotController.php:27-32` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-087-04 | BR-087 | Throttle chỉ route chatbot | API Feature / Middleware | Một client gửi trên 20 request/phút | Gọi `/api/chatbot` và `/api/travel-assistant` theo ngưỡng | `/api/chatbot` bị throttle theo `20,1`; route travel-assistant không có throttle riêng trong route declaration | `routes/api.php:57,154` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-088-01 | BR-088 | Giới hạn context history/tour và cấu hình Gemini | Integration / External API fake | Conversation có >10 messages; DB có >10 tour published và tour non-published; `Http::fake()` | Gọi chatbot và inspect request fake | Prompt chỉ chứa 10 history gần nhất và tối đa 10 tour published; URL model `gemini-2.5-flash`; timeout 15; temperature 0.2 | `ChatBotController.php:33-46,132-199` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-088-02 | BR-088 | Áp đúng các filter được nhận diện | Integration / Data set | Tour có/không discount, biển/núi, số ngày/đêm; message lần lượt chứa pattern được BR liệt kê | Gọi chatbot, inspect danh sách tour trong prompt | Query chỉ áp discount/terrain/days/nights theo pattern source; dị ứng/phấn hoa được extract theo source nhưng không tự bổ sung filter ngoài code | `ChatBotController.php:71-129`, `extractFilters()`, `buildTourQuery()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-089-01 | BR-089 | Lưu câu hỏi/trả lời khi Gemini thành công | Integration / Happy path | HTTP fake trả candidate text | Gọi chatbot | Insert user message và assistant message `is_fallback=false`; response trả assistant cùng session | `ChatBotController.php:48-68,176-204`; chat message migration | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-089-02 | BR-089 | HTTP error dùng fallback và warning log | Integration / Exception | HTTP fake 500; spy log | Gọi chatbot | Không trả lỗi external; log warning; lưu cả hai messages; assistant bằng fallback và `is_fallback=true` | `ChatBotController.php:48-68,201-212`, `callGemini()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-089-03 | BR-089 | Exception/no text dùng fallback đúng nhánh log | Integration / Data set | Một case HTTP ném Throwable; một case success nhưng không có text | Gọi chatbot | Throwable ghi error; response không text dùng fallback; cả hai lưu assistant fallback | `ChatBotController.php:176-212` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-090-01 | BR-090 | Công thức doanh thu overview | API Feature / Aggregate | Admin; payments success/failed ở nhiều năm; booking cancelled/non-cancelled | GET `/api/admin/reports/overview?year=2026` | Chỉ tổng `payments.amount` success, paid_at năm 2026, booking không cancelled | `ReportController.php:10-61`, `getOverviewStatistics()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-090-02 | BR-090 | Chart booking/customer và top destination | API Feature / Aggregate | Admin; booking/user theo tháng; destination có nhiều booking kể cả cancelled | GET `/api/admin/reports/charts?year=2026` | Booking/customer nhóm theo `created_at`; top destination loại cancelled, sắp theo count và tối đa 5 | `ReportController.php:63-163`, `getChartStatistics()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-090-03 | BR-090 | Chỉ admin đọc báo cáo | API Feature / Authorization | Guest và customer | Gọi hai report endpoints | Guest `401`; customer `403` | `routes/api.php:197-198`; admin route group | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-091-01 | BR-091 | List/search/show user | API Feature / Read | Admin; users nhiều role/status | Gọi list, search và show | HTTP `200`; filter/search và detail trả user phù hợp | `CustomerManagerController.php:19-151`, `index()`, `search()`, `show()` | `backend_laravel/tests/Feature/RbacAuthorizationTest.php` — `admin can access admin customer APIs`; chỉ assert GET list `200` và `status=success`, chưa cover search/show |
| TC-SA-091-02 | BR-091 | Create user luôn active | API Feature / Create | Admin; email unique, role tồn tại, payload hợp lệ | POST `/api/admin/customers` | User insert với `status=active`, password hash; relation guide/support sync theo role trong transaction | `CustomerManagerController.php:153-245`, `store()`, `syncRoleRelations()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-091-03 | BR-091 | Update user và đồng bộ relation theo role | API Feature / Update + Transaction | Admin; user hiện có guide/support relation | PUT user đổi role và fields | User và relation tương ứng update/restore/soft-delete cùng transaction; response success | `CustomerManagerController.php:247-426`, `update()`, `syncRoleRelations()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-091-04 | BR-091 | Lock/unlock và nhánh lặp | API Feature / State | Admin; user active | PATCH lock hai lần; PATCH unlock hai lần | Lần đầu mỗi action đổi inactive/active và `200`; lần lặp trả `422`; không có delete user | `CustomerManagerController.php:428-506`, `lock()`, `unlock()`; routes `api.php:216-231` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-091-05 | BR-091 | Authorization customer manager | API Feature / Authorization | Guest, customer, admin | GET `/api/admin/customers` | Guest `401`; customer `403`; admin `200` | `routes/api.php:216-231`; `CheckRole.php` | `RbacAuthorizationTest.php` — `admin customer APIs require authentication`, `customer cannot access admin customer APIs`, `admin can access admin customer APIs`; assert lần lượt `401`, `403`, `200` |
| TC-SA-092-01 | BR-092 | Guide CRUD và quan hệ phụ | API Feature / CRUD | Admin; user guide hợp lệ; destination/language/certificate hợp lệ | Create, show, update guide | Insert/update guide, destination pivot, languages/experiences đúng payload; validation sai trả `422` | `GuideController.php:14-829`, CRUD methods; guide migrations | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-092-02 | BR-092 | Guide trash/restore/force delete | API Feature / Lifecycle | Admin; guide tồn tại | DELETE, GET trash, restore; xóa lại rồi force | Soft delete đặt deleted_at; trash chứa row; restore; force xóa row và dữ liệu liên quan theo controller transaction | `GuideController.php`, `destroy()`, `trashed()`, `restore()`, `forceDelete()`; routes `api.php:234-247` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-092-03 | BR-092 | SupportStaff CRUD và lifecycle | API Feature / CRUD + Soft delete | Admin; user role support staff hợp lệ | Create/show/update/delete/trash/restore/force | DB thay đổi theo từng method; model soft delete; force route xóa vật lý | `SupportStaffController.php:14-480`; `SupportStaff.php:10-35`; routes `api.php:278-299` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-092-04 | BR-092 | Upload/delete avatar Guide và SupportStaff | API Feature / Filesystem | Admin; profile và user có avatar cũ; file JPG/PNG/WebP ≤2.048 KB | Upload rồi delete avatar cho từng module | File/`users.avatar_url` được cập nhật theo method; invalid type/size trả `422` | `GuideController.php`, `SupportStaffController.php`, avatar methods | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-092-05 | BR-092 | Chỉ admin quản lý profile | API Feature / Authorization | Guest và customer | Gọi list Guide/SupportStaff | Guest `401`; customer `403` | routes admin guides/support-staff; `CheckRole.php` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-093-01 | BR-093 | Category CRUD, trash và restore | API Feature / Lifecycle | Admin; category hợp lệ | Create/read/update/delete/list trash/restore | CRUD thành công; delete mềm; trash/restore hoạt động | `CategoryController.php:12-227`; `Category.php` SoftDeletes; routes `api.php:319-325` | `RbacAuthorizationTest.php` — `customer cannot access any admin-only category API` chỉ assert customer GET list `403`; chưa cover CRUD/lifecycle |
| TC-SA-093-02 | BR-093 | Destination CRUD, trash, restore và force | API Feature / Lifecycle | Admin; destination hợp lệ | Thực hiện đầy đủ endpoints | CRUD, soft delete, trash, restore và force delete đúng route | `DestinationController.php:9-207`; `Destination.php`; routes `api.php:303-316` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-093-03 | BR-093 | ServiceCategory create/update và slug/unique | API Feature / CRUD + Validation | Admin; tên tiếng Việt; duplicate kể cả soft-deleted | Create, update name, thử duplicate | Slug tự sinh/đổi; duplicate name bị `422`; row đúng DB | `ServiceCategoryController.php`, `ServiceCategoryService.php`, FormRequests, model callback | `ServiceCategoryApiTest.php` — `admin can create service category with generated slug`, `service category name must be unique including soft deleted records`, `updating service category name changes slug`, `generated slug avoids duplicates after normalization`; assert response và DB tương ứng |
| TC-SA-093-04 | BR-093 | ServiceCategory search/filter/pagination | API Feature / Read | Admin; dữ liệu active/inactive >1 page | GET với search, status, page/per_page | Chỉ trả items phù hợp và pagination đúng | ServiceCategory index request/service | `ServiceCategoryApiTest.php` — `search by name works`, `status filters work for active and inactive service categories`, `pagination works`; assert count/field/pagination |
| TC-SA-093-05 | BR-093 | ServiceCategory soft delete và không có force route | API Feature / Route contract | Admin; service category tồn tại | DELETE; GET detail; DELETE `/force` | Row soft-deleted; detail `404`; force endpoint `404` và không hard-delete | ServiceCategory `destroy()`; `routes/api.php:191-194` | `ServiceCategoryApiTest.php` — `delete uses soft delete`, `show returns not found after soft delete`, `force delete endpoint is not available`; assert soft delete, `404`, DB row còn |
| TC-SA-093-06 | BR-093 | Authorization ServiceCategory | API Feature / Authorization | Customer | GET list | HTTP `403` với response error | admin service-category route group | `ServiceCategoryApiTest.php` — `non admin user receives forbidden response`; assert `403`, success false và message |
| TC-SA-094-01 | BR-094 | Create language cùng nested levels trong transaction | API Feature / Transaction | Admin; language unique và nhiều level hợp lệ | POST language; một case ép insert level lỗi | Thành công tạo language/levels; case lỗi rollback cả language và levels | `LanguageController.php:11-210`, `store()`; language migrations | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-094-02 | BR-094 | CRUD level và kiểm tra trùng theo language | API Feature / Nested CRUD | Admin; hai languages; level A đã tồn tại ở language 1 | List/store/update/delete level; thử trùng trong cùng language và cùng tên ở language khác | Trùng cùng language bị `422`; cùng tên khác language được DB chấp nhận; CRUD đúng | `LanguageController.php`, `levels()`, `storeLevel()`, `updateLevel()`, `destroyLevel()`; migration không composite unique | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-094-03 | BR-094 | Unique language/certificate tại DB | Integration / Constraint | Admin; đã có tên language/certificate | Gửi duplicate và thử insert trực tiếp bỏ qua validator | API `422`; insert trực tiếp vi phạm unique DB cho hai bảng; language_levels không có constraint cặp | Language/certificate migrations `unique()`; controllers validation | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-094-04 | BR-094 | Certificate CRUD và chặn delete khi đang dùng | API Feature / Lifecycle | Admin; certificate tự do và certificate có guide experience | CRUD; delete hai loại | Certificate tự do xóa được; loại đang dùng trả `422` và row còn | `CertificateController.php:9-112`, CRUD methods | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-095-01 | BR-095 | Chỉ ghi setting trong ALLOWED_KEYS | API Feature / Allowlist | Admin; payload có key hợp lệ và key lạ | PUT settings | Chỉ key hợp lệ được upsert và trả response; key lạ không xuất hiện DB | `Setting.php:7-84`; `SettingController.php:21-103` | `BackupSettingsTest.php` — `admin can save backup settings`, `admin can save security and locale settings`; assert các key hợp lệ và group, chưa có assertion key lạ |
| TC-SA-095-02 | BR-095 | Validate từng nhóm settings | API Feature / Validation | Admin; security/locale/backup values ngoài allowlist/range | PUT settings | HTTP `422` đúng field; không ghi giá trị sai | `SettingController.php:56-86` | `BackupSettingsTest.php` — `backup settings are validated` và `security and locale settings are validated`; assert `422` cùng toàn bộ field được gửi sai |
| TC-SA-095-03 | BR-095 | Public settings không lộ key private | API Feature / Data exposure | DB có `require_2fa` và `currency` | Guest GET `/api/settings/public` | Trả currency; không có require_2fa | `PublicSettingController.php:9-28`; `Setting::PUBLIC_KEYS` | `BackupSettingsTest.php` — `public settings do not expose security configuration`; assert currency hiện diện, require_2fa vắng |
| TC-SA-095-04 | BR-095 | CRUD/toggle widget image và scope visible | API Feature / CRUD + State | Admin; widget image có date/status hợp lệ; guest | Admin create/show/update/toggle/delete; guest GET public trong/ngoài date | CRUD/toggle thay DB; public chỉ trả active đúng page/date theo `scopeVisible()` | `WidgetController.php:11-162`; `PublicWidgetController.php:10-38`; `Banner.php:8-61` | `RbacAuthorizationTest.php` — `public routes remain public` chỉ assert `/api/widgets` trả `200`, chưa assert visible/filter/CRUD |
| TC-SA-095-05 | BR-095 | Tạo widget HTML không có image | API Feature / Business mismatch | Admin; schema migration thật; `type=html`, có html_content, không image_url | POST `/api/admin/widgets` | Theo BR: tạo thành công. Hiện trạng payload gán null vào cột NOT NULL nên DB từ chối: **BUG-SA-002** | `WidgetController.php:31-41,113-152`; `backend_laravel/database/migrations/2026_06_10_220190_create_banners_table.php:14-27`; migration widget columns | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-095-06 | BR-095 | Authorization settings/widgets | API Feature / Authorization | Guest, customer, admin | Gọi admin settings/widget và public endpoints | Admin được quản lý; guest `401`, customer `403` ở admin; public endpoints không yêu cầu token | routes `api.php:178-179,352-361`; `CheckRole.php` | `RbacAuthorizationTest.php` — `public routes remain public` assert public settings/widgets `200`; chưa có test auth riêng cho admin settings/widgets |
| TC-SA-096-01 | BR-096 | Admin create booking với default/payment COD | API Feature / Transaction | Admin; customer/tour/departure đủ slot; contact/participants hợp lệ | POST `/api/admin/bookings` | Transaction tạo booking `pending/unpaid`, contact/participants và payment COD `pending`, paid_at null | `Admin/BookingController.php:19-207`, `store()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-096-02 | BR-096 | Update participant và tính lại tổng giá | API Feature / Pricing | Admin; booking chưa cancelled; departure/tour có pricing; payload participants mới | PUT booking | Xóa/reinsert participant theo source, tính unit price/total_amount mới và cập nhật contact/booking trong transaction | `Admin/BookingController.php:209-285`, `update()`; `TourPricingService` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-096-03 | BR-096 | Cấm đổi payment_status trực tiếp | API Feature / Validation | Admin; booking unpaid | PUT với `payment_status=paid` | HTTP `422`; booking vẫn unpaid | `BookingController.php:214-260`, validation `prohibited` | `backend_laravel/tests/Feature/PaymentBookingSafetyTest.php` — `booking update cannot change payment status directly`; assert `422` và DB vẫn unpaid |
| TC-SA-096-04 | BR-096 | Dedicated cancel hoàn slot đúng một lần | API Feature / Idempotency + Lock | Admin; booking 2 người, departure booked_slots=2 | PATCH cancel hai lần tuần tự | Cả hai response OK theo source; booked_slots về 0 sau lần đầu và không giảm lần hai | `BookingController.php:295-311,434-449`, `softDelete()`, `releaseBookedSlots()` | `PaymentBookingSafetyTest.php` — `cancel booking releases slots only once`; assert hai PATCH `200` và booked_slots vẫn 0 |
| TC-SA-096-05 | BR-096 | Không hoàn slot lần hai sau reactivation | API Feature / Business mismatch | Booking confirmed 2 người; departure còn booking khác để quan sát số đếm | Cancel; PUT booking về confirmed; cancel lại | Theo BR: chỉ lần hủy đầu hoàn 2 slot. Source trừ thêm lần hai vì không lưu cờ release: **BUG-SA-003** | `BookingController.php:211-272,295-311,434-449` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-096-06 | BR-096 | Hai PUT cancel đồng thời chỉ hoàn một lần | Concurrency / Business mismatch | Booking confirmed; hai kết nối cùng load trước update | Gửi đồng thời hai PUT status cancelled | Theo BR: departure chỉ trừ một lần. Source không lock booking trong `update()` và cả hai gọi release: **BUG-SA-003** | `BookingController.php:211,263-272,434-449` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-096-07 | BR-096 | Chỉ hard delete booking cancelled | API Feature / State guard | Admin; một booking pending và một cancelled | DELETE từng booking | Pending trả `422` và còn DB; cancelled bị xóa vật lý | `BookingController.php:313-337`, `destroy()`; model không SoftDeletes | `PaymentBookingSafetyTest.php` — `cannot permanently delete booking before it is cancelled`; assert pending `422`, message và row còn; chưa cover success cancelled |
| TC-SA-096-08 | BR-096 | Authorization booking admin | API Feature / Authorization | Guest và customer | GET `/api/admin/bookings` | Guest `401`; customer `403` | `routes/api.php:376-383`; `CheckRole.php` | `PaymentBookingSafetyTest.php` — `guest and non admin cannot access admin booking and payment routes`; assert booking guest `401`, customer `403` |

## Truy vết BUG qua test

| BUG | Test case phát hiện |
| --- | --- |
| BUG-SA-002 | TC-SA-095-05 |
| BUG-SA-003 | TC-SA-096-05, TC-SA-096-06 |
