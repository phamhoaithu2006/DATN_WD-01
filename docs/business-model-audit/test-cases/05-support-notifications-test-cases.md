# Test Case — Hỗ trợ và thông báo

## Phạm vi

- Business Rules: `BR-074`–`BR-084`.
- Mỗi kết quả mong đợi dưới đây xuất phát từ Business Model và source đã truy vết; các case gắn BUG thể hiện kết quả nghiệp vụ mong muốn và sai lệch hiện tại.
- Test chuyên biệt cho support request, notification customer/support hoặc campaign notification trong `backend_laravel/tests`: **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** tại baseline audit.

## Danh sách test case

| ID | Business Rule | Mục tiêu | Loại test | Tiền điều kiện và dữ liệu | Các bước | Kết quả mong đợi | Source Code Reference | Hiện trạng tự động hóa |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TC-SA-074-01 | BR-074 | Tạo ticket với payload tối thiểu hợp lệ | API Feature / Happy path | Customer đã đăng nhập; không gửi `phone`, `attachments`; category `technical` | Gọi `POST /api/customer/support-requests` với name, email, subject, description hợp lệ | HTTP `201`; `success=true`; tạo một support request của customer; phone null; không có attachment | `CustomerSupportRequestController.php:25-185`, `store()`; route `api.php:119` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-074-02 | BR-074 | Kiểm tra biên trên description và attachment | API Feature / Boundary | Customer; description đúng 10.000 ký tự; 5 file, mỗi file đúng 5.120 KB, thuộc JPG/JPEG/PNG/WebP/PDF/DOC/DOCX | Gửi multipart request | HTTP `201`; lưu đủ 5 attachment và metadata tương ứng | `CustomerSupportRequestController.php:64-82,142-159`; migrations support attachment | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-074-03 | BR-074 | Từ chối từng nhóm input ngoài contract | API Feature / Validation / Data set | Customer; chạy các bộ: thiếu required, category ngoài allowlist, description 10.001, 6 file, file sai MIME, file 5.121 KB, phone 21 ký tự | Gửi từng payload | HTTP `422`; có validation error đúng field; không tạo support request/file | `CustomerSupportRequestController.php:33-83`, `store()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-074-04 | BR-074 | Chặn actor không phải customer | API Feature / Authorization | Một guest; một user admin; payload hợp lệ | Mỗi actor gọi endpoint tạo ticket | Guest `401`; admin `403`; không có DB insert | `routes/api.php:95,119`; `CheckRole.php:11-37` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-075-01 | BR-075 | Xác minh mã ticket và trạng thái mặc định | API Feature / Database | Customer; schema có cột priority; payload hợp lệ | Tạo ticket, đọc row DB | `ticket_code` khớp `^SUP-YYYYMMDD-[A-Z0-9]{6}$`; status `pending`; assigned/start/resolve null; priority `medium` | `CustomerSupportRequestController.php:101-135,203-217`; `backend_laravel/database/migrations/2026_07_16_220919_create_support_requests_table.php` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-075-02 | BR-075 | Sinh lại mã khi candidate đã tồn tại | Unit/Integration / Collision | Đóng băng ngày; chuẩn bị row có candidate đầu; điều khiển chuỗi random để lần đầu trùng, lần sau khác | Kích hoạt tạo ticket | Query phát hiện candidate trùng; ticket mới dùng candidate thứ hai; unique DB không bị vi phạm | `CustomerSupportRequestController.php:203-217`, `generateTicketCode()`; unique `ticket_code` trong migration | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-075-03 | BR-075 | Rollback toàn bộ DB khi một bước trong callback lỗi | Integration / Transaction | Customer; có attachment và support recipient; ép insert notification ném exception | Gọi create ticket | Request, attachment record và notification đều không commit; exception thoát khỏi controller | `CustomerSupportRequestController.php:89-195`, `DB::transaction(...,3)` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-075-04 | BR-075 | Ghi nhận hành vi double-submit không idempotent | API Feature / Idempotency characterization | Customer; cùng một payload hợp lệ; không có idempotency key | Gọi endpoint hai lần tuần tự | Tạo hai ticket với hai mã khác nhau; đây là hành vi source, không có cơ chế khử trùng request | `CustomerSupportRequestController.php:90-173,203-217` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-076-01 | BR-076 | Xóa file đã lưu khi transaction thất bại | Integration / Rollback filesystem | `Storage::fake('public')`; một file hợp lệ; ép lỗi DB sau khi `store()` trả path | Gửi ticket, bắt exception, kiểm tra DB và disk | DB rollback; tất cả path trong `$storedPaths` không còn trên disk public | `CustomerSupportRequestController.php:87-95,142-159,186-195`, `store()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-077-01 | BR-077 | Thông báo từng support profile active và khử trùng ID | Integration / Notification | Có hai support profile active và một profile inactive; schema notifications đầy đủ | Customer tạo ticket | Mỗi user active nhận đúng một notification unread `type=system`; inactive không nhận; response `notified_staff_count=2` | `CustomerSupportRequestController.php:222-270,291-312`, `notifySupportStaff()`, `getSupportUserIds()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-077-02 | BR-077 | Fallback sang role support khi không tìm thấy profile active | Integration / Alternative flow | Không có support profile active; có user role name `support staff` | Customer tạo ticket | User role support nhận notification unread; count phản ánh số ID role tìm được | `CustomerSupportRequestController.php:319-363`, `getSupportUserIds()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-077-03 | BR-077 | Không có recipient vẫn tạo ticket | API Feature / Empty branch | Không có profile/user role support; customer hợp lệ | Tạo ticket | HTTP `201`; ticket được commit; `notified_staff_count=0`; không có notification | `CustomerSupportRequestController.php:224-228,167-185` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-078-01 | BR-078 | Chuyển ticket về pending | API Feature / State | Support staff; ticket đang resolved với assignee và timestamps | `PATCH /api/support/requests/{id}/status`, status `pending` | HTTP `200`; status pending; `assigned_to`, `started_at`, `resolved_at` đều null | `SupportRequestController.php:123-177`, nhánh dòng 140-144 | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-078-02 | BR-078 | Chuyển ticket sang in_progress | API Feature / State | Support staff A; ticket pending, chưa start | PATCH status `in_progress`, sau đó gọi lặp lại | Gán A; tạo `started_at`; clear resolved; lần lặp giữ nguyên started_at | `SupportRequestController.php:146-152`, `updateStatus()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-078-03 | BR-078 | Resolve trực tiếp và resolve lặp | API Feature / State | Support staff A; ticket pending, chưa assignee/start | PATCH `resolved`, ghi nhận timestamp; PATCH `resolved` lần nữa | Lần đầu gán A, tạo start và resolve; lần hai giữ assignee/start nhưng thay `resolved_at` bằng thời điểm mới | `SupportRequestController.php:154-166`, `updateStatus()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-078-04 | BR-078 | Không áp transition order và chỉ nhận ba status | API Feature / Alternative + Validation | Support staff; ticket resolved | PATCH thẳng `in_progress`; sau đó PATCH `closed` | Transition resolved→in_progress được chấp nhận và clear resolved_at; `closed` trả `422` | `SupportRequestController.php:127-166`; BR-078 nêu không có transition guard | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-078-05 | BR-078 | Chặn actor sai role | API Feature / Authorization | Guest và customer; ticket tồn tại | Gọi PATCH status hợp lệ | Guest `401`; customer `403`; ticket không đổi | `routes/api.php:87-91`; `CheckRole.php:11-37` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-079-01 | BR-079 | Badge chỉ cộng pending và in_progress | API Feature / Aggregate | Support staff; tạo 2 pending, 3 in_progress, 4 resolved | Gọi `GET /api/support/requests/badge-count` | HTTP `200`; `count=5` | `SupportRequestController.php:106-117`, `badgeCount()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-079-02 | BR-079 | Badge chỉ dành cho support staff | API Feature / Authorization | Guest, customer và support staff | Mỗi actor gọi badge | Guest `401`; customer `403`; support staff `200` | `routes/api.php:80,87-91`; `CheckRole.php:11-37` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-080-01 | BR-080 | List/count chỉ gồm notification sở hữu | API Feature / Ownership | User A có 2 unread, 1 read; user B có 3 unread | A gọi list và unread-count | List không chứa row B; unread count của A bằng 2 | `NotificationCustomerController.php:19-36,89-116` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-080-02 | BR-080 | Detail và mark-read cập nhật đúng metadata | API Feature / State | User A có notification unread | GET detail; ghi nhận `read_at`; GET detail lần nữa; với row unread khác gọi PATCH read | Detail đầu đặt read/time; detail lặp không đổi time; PATCH đặt read/time và trả message | `NotificationCustomerController.php:40-85` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-080-03 | BR-080 | Không đọc/sửa notification user khác | API Feature / Authorization ownership | A đăng nhập; notification thuộc B | A gọi detail và mark-read bằng ID của B | Cả hai trả `404`; row của B không đổi | `NotificationCustomerController.php:45-50,72-83,101-116` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-080-04 | BR-080 | Ẩn guide-review request với actor non-customer | API Feature / Role filtering | Guide/admin/support có một notification `data.kind=guide_review_request` và một notification thường; customer có cùng hai loại | Mỗi actor gọi list/count/detail | Non-customer chỉ thấy notification thường và detail review-request trả `404`; customer thấy cả hai | `NotificationCustomerController.php:22-30,101-121`, `visibleNotificationsQuery()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-081-01 | BR-081 | Tạo, xem và sửa draft hợp lệ | API Feature / CRUD | Admin; target lần lượt all/role/specific, target IDs nullable/array | POST draft, GET detail, PUT draft | HTTP `200`; DB giữ title/message/target/status `draft`; target IDs cast về array | `NotificationController.php:15-103`; `NotificationDraft.php:8-13` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-081-02 | BR-081 | Validation draft | API Feature / Validation | Admin; data set thiếu title/message, title 256, target ngoài enum, target_ids scalar | POST/PUT từng payload | HTTP `422`; không insert/update draft | `NotificationController.php:18-23,84-90` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-081-03 | BR-081 | Không xem/sửa sent bằng method draft | API Feature / State guard | Admin; draft status `sent` | GET và PUT `/draft/{id}` | Cả hai trả `404`; row không đổi | `NotificationController.php:58-66,75-82` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-081-04 | BR-081 | Soft delete, trash, restore và force delete | API Feature / Lifecycle | Admin; draft tồn tại | DELETE thường; GET trash; POST restore; DELETE thường; DELETE force | `deleted_at` được đặt; trash chứa row; restore clear `deleted_at`; force xóa row vật lý | `NotificationController.php:106-168`; `NotificationDraft` dùng `SoftDeletes`; migration deleted_at | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-081-05 | BR-081 | Authorization toàn bộ draft API | API Feature / Authorization | Guest và customer; draft tồn tại | Gọi một endpoint đọc và một endpoint ghi | Guest `401`; customer `403`; không đổi DB | `routes/api.php:389-413`; `CheckRole.php:11-37` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-082-01 | BR-082 | Gửi target all trong một transaction | API Feature / Happy path | Admin; draft `all`; 3 user | POST `/api/admin/notifications/send/{id}` | HTTP `200`; đúng 3 notification unread `type=system`, cùng draft_id; draft thành `sent` | `NotificationController.php:171-218`, `sendNotification()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-082-02 | BR-082 | Resolve target role và specific | API Feature / Data set | Admin; draft role chứa role IDs; draft specific chứa user IDs; có user ngoài target | Gửi từng draft | Chỉ user thuộc role/ID target nhận row; mỗi draft thành sent | `NotificationController.php:183-207`, match target | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-082-03 | BR-082 | Nhánh không recipient và gửi lại tuần tự | API Feature / Exception + Idempotency | Admin; một draft target không khớp ai; một draft đã gửi thành công | Gửi draft rỗng; gửi lại draft sent | Cả hai trả `404`; draft rỗng vẫn `draft`; không tạo thêm notification khi gửi lại | `NotificationController.php:177-193,210-213` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-082-04 | BR-082 | Rollback bulk insert khi cập nhật draft lỗi | Integration / Transaction | Admin; draft hợp lệ; ép câu update status ném exception sau insert | Gửi draft và kiểm tra bằng kết nối khác sau rollback | Không còn notification của draft; draft vẫn `draft` | `NotificationController.php:175-218`, `DB::transaction()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-082-05 | BR-082 | Một draft không được gửi hai lần khi request đồng thời | Concurrency / Data Integrity | Admin; draft specific một user; hai kết nối cùng mở transaction và cùng đọc trước update | Gửi đồng thời hai POST cho cùng ID | Theo BR/FR: chỉ một request bulk insert thành công, request còn lại đi nhánh `404`; đúng một row `(draft_id,user_id)`. Source hiện cho cả hai insert/`200`: **BUG-SA-004** | `NotificationController.php:175-213`; migrations notification không unique `(draft_id,user_id)`; `04-srs.md:181-184` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-083-01 | BR-083 | Thu hồi campaign sent | API Feature / Happy path | Admin; draft sent có 3 notifications cùng draft_id | DELETE `/api/admin/notifications/revoke/{draft_id}` | HTTP `200`; xóa 3 notification; draft về `draft`; message chứa count 3 | `NotificationController.php:250-274`, `revoke()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-083-02 | BR-083 | Chỉ campaign sent được revoke | API Feature / State guard | Admin; một draft `draft`, một ID không tồn tại | Revoke từng ID | HTTP `404`; không xóa notification ngoài phạm vi | `NotificationController.php:253-262` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-083-03 | BR-083 | Chứng minh không rollback giữa delete và update | Integration / Failure characterization | Admin; draft sent có notification; ép update draft ném exception sau delete | Gọi revoke, bắt exception, đọc DB | Notification đã bị xóa nhưng draft còn `sent`; không có transaction/rollback theo đúng mô tả BR-083 | `NotificationController.php:264-269`, không có `DB::transaction()` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-083-04 | BR-083 | Quan sát hai revoke đồng thời | Concurrency / Idempotency characterization | Draft sent có notifications; hai kết nối cùng đọc trạng thái sent trước delete | Gọi đồng thời hai DELETE | Cả hai vượt initial guard; một request xóa N row, request còn lại xóa 0; cả hai đưa draft về `draft`. Đây là hành vi source do không lock/transaction | `NotificationController.php:251-274` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-084-01 | BR-084 | Support gửi thông báo tới tất cả admin | API Feature / Business mismatch | Support staff; hai admin; schema tạo đúng migration | POST `/api/notifications/support/send` với title/message hợp lệ | Theo BR: HTTP `200`, mỗi admin nhận một unread notification có metadata sender. Hiện trạng insert `type=support_message` trái enum và transaction rollback: **BUG-SA-001** | `SupportNotificationController.php:113-158`; `backend_laravel/database/migrations/2026_06_10_220130_create_notifications_table.php:14-23` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-084-02 | BR-084 | Không có admin recipient | API Feature / Empty branch | Support staff; không có user role admin | Gọi endpoint send | HTTP `404`, `success=false`; không insert notification | `SupportNotificationController.php:120-131` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-084-03 | BR-084 | Validation title/message | API Feature / Validation | Support staff; data set thiếu title/message và title 256 | Gửi từng payload | HTTP `422`; không insert notification | `SupportNotificationController.php:113-118` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |
| TC-SA-084-04 | BR-084 | Chỉ support staff được gửi | API Feature / Authorization | Guest, customer, guide, admin; payload hợp lệ | Mỗi actor gọi endpoint | Guest `401`; các role không phải support staff `403`; không insert | `routes/api.php:139-147`; `CheckRole.php:11-37` | **KHÔNG TÌM THẤY BẰNG CHỨNG TRONG SOURCE CODE** |

## Truy vết BUG qua test

| BUG | Test case phát hiện |
| --- | --- |
| BUG-SA-001 | TC-SA-084-01 |
| BUG-SA-004 | TC-SA-082-05 |
